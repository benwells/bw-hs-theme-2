# IMPORTANT: js/dist/ files are COMMITTED
# HubSpot needs these built files to function
# The .gitignore is configured to track them

# To rebuild components:
bun run build

# To deploy:
bun run deploy
