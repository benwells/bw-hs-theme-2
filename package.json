{
  "name": "my-first-theme",
  "version": "1.0.0",
  "description": "Modern HubSpot theme with Svelte web components",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "watch": "vite build --watch",
    "deploy": "bun run build && hs upload . my-first-theme"
  },
  "devDependencies": {
    "@hubspot/cli": "^6.0.0",
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "svelte": "^4.2.0",
    "vite": "^5.0.0"
  }
}